{
  "name": "static",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "elm-test",
    "build": "cd src && npx elm-make Main.elm --yes --output ../main.js",
    "watch": "watch 'npm run build' ./src -p=/elm-stuff/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-test": "^0.18.12"
  }
}
